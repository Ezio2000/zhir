# zhir-builtins

Enable `filesystem`, `shell`, `interaction` or `agent` explicitly. Builtins use the same RuntimeTool trait and registration path as application tools. Only the agent feature depends on zhir-kernel.

Part of the zhir Cargo workspace, version 0.1.0.
